n = int(input())  # 첫 번째 줄에서 자연수 N을 입력받기
p = dict()  # 단어들을 저장할 딕셔너리 p를 초기화

# 첫 번째 입력 부분: 노트에 적어놓은 N개의 단어를 입력받아 딕셔너리에 저장합니다.
for i in range(n):
    word = input()
    p[word] = 1  # 각 단어를 키로 하고 값을 1로 설정합니다.

# 두 번째 입력 부분: 시에 사용된 N-1개의 단어를 입력받아 딕셔너리에서 값을 0으로 설정합니다.
for i in range(n-1):
    word = input()
    p[word] = 0  # 각 단어를 키로 하고 값을 0으로 설정합니다.

# 시에 쓰이지 않은 단어를 찾아 출력합니다.
for key, val in p.items():
    if val == 1:  # 값이 1인 키를 찾으면, 그 키가 시에 쓰이지 않은 단어입니다.
        print(key)
        break  # 찾으면 바로 출력하고 반복문을 종료합니다.
